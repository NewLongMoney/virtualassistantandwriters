/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['instagram.com', 'scontent.cdninstagram.com', 'cdninstagram.com'],
  },
};

export default nextConfig;
